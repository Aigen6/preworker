# Database Configuration
DATABASE_DSN=host=localhost user=zkpay dbname=zkpay port=5432 sslmode=disable TimeZone=UTC

# Server Configuration
SERVER_HOST=localhost
SERVER_PORT=3001

# CORS Configuration
# If using Nginx or other reverse proxy to manage CORS, keep default "*"
# If backend is directly exposed to frontend, set specific domain, e.g.: https://yourdomain.com
# Multiple domains separated by commas
# CORS_ALLOWED_ORIGINS=*

# ZKVM Service Configuration
ZKVM_BASE_URL=http://localhost:8081

# Blockchain Scanner Configuration
# Development: http://localhost:18080
# Production: http://zkpay-blockscanner:18080 (Docker Compose service name)
SCANNER_BASE_URL=http://localhost:18080

# BSC Mainnet Configuration
BSC_PRIVATE_KEY=your_private_key_here_without_0x_prefix
BSC_RELAYER_ADDRESS=0x...  # Optional, auto-derived from private key
BSC_RPC_ENDPOINTS=https://bsc-dataseed1.binance.org/,https://bsc-dataseed2.binance.org/
BSC_GAS_PRICE=auto  # Dynamic (recommended) or set specific value like "10000000000"
BSC_GAS_LIMIT=1000000  # SP1 verification requires more gas

# BSC Testnet Configuration (optional)
BSC_TESTNET_PRIVATE_KEY=your_testnet_private_key_here
BSC_TESTNET_RPC_ENDPOINTS=https://data-seed-prebsc-1-s1.binance.org:8545/
BSC_TESTNET_GAS_PRICE=auto  # Dynamic (recommended) or set specific value like "20000000000"
BSC_TESTNET_GAS_LIMIT=1000000 