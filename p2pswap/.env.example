# ============================================
# P2PSwap 统一环境变量配置
# ============================================

# ========== KeyManager 配置 ==========
# KeyManager 主种子密钥（必需）
MASTER_SEED=Dinaswap.xyz

# KeyManager API 服务端口（可选，默认 8080）
KEYMANAGER_PORT=8080

# ========== Frontend 配置 ==========
# Next.js 端口（可选，默认 5173）
FRONTEND_PORT=5173

# ========== Enclave Backend 配置 ==========
# Enclave Backend API URL（必需）
NEXT_PUBLIC_API_URL=http://localhost:3001

# Enclave Backend WebSocket URL（可选）
NEXT_PUBLIC_WS_URL=ws://localhost:3001/ws

# ========== KeyManager API 配置 ==========
# KeyManager API 服务地址（前端使用）
NEXT_PUBLIC_KEYMANAGER_API_URL=http://localhost:8080

# ========== WalletConnect 配置（可选） ==========
# 注意：Wallet SDK 是作为 npm 包直接使用的，不需要 URL 配置
# 如果需要使用 WalletConnect 功能，需要配置 Project ID
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your-walletconnect-project-id

# ========== Deposit Vault 地址（推荐，直接配置） ==========
# 按链配置 DepositVault 合约地址（推荐方式，最简单）
# 如果配置了这个，就不需要 TreasuryConfigCore
NEXT_PUBLIC_DEPOSIT_VAULT_1=0x...   # Ethereum
NEXT_PUBLIC_DEPOSIT_VAULT_714=0x...   # BSC
NEXT_PUBLIC_DEPOSIT_VAULT_195=0x...   # TRON

# ========== Treasury Config Core 地址（可选，回退方案） ==========
# 注意：如果配置了 DepositVault 地址，就不需要这个
# 只有当需要从合约读取 DepositVault 地址时才需要配置
# 按链配置 TreasuryConfigCore 合约地址
# NEXT_PUBLIC_TREASURY_CONFIG_CORE_60=0x...   # Ethereum (Chain ID 1)
# NEXT_PUBLIC_TREASURY_CONFIG_CORE_714=0x...   # BSC (Chain ID 56)
# NEXT_PUBLIC_TREASURY_CONFIG_CORE_195=0x...   # TRON

# ========== RPC 节点配置 ==========
# 以太坊 RPC URL（可选，有默认值）
NEXT_PUBLIC_ETH_RPC=https://eth.llamarpc.com

# BSC RPC URL（可选，有默认值）
NEXT_PUBLIC_BSC_RPC=https://bsc-dataseed.binance.org

# Polygon RPC URL（可选）
NEXT_PUBLIC_POLYGON_RPC=https://polygon-rpc.com

# TRON RPC URL（可选）
NEXT_PUBLIC_TRON_QUERY_RPC_URL=https://api.trongrid.io

# ========== TRON Energy 配置 ==========
# TRON Energy 租赁 API（可选）
NEXT_PUBLIC_ENERGY_RENTAL_API_URL=http://localhost:4001

# TRON Energy 配置（可选）
NEXT_PUBLIC_TRON_ENERGY_APPROVE_ENERGY=...
NEXT_PUBLIC_TRON_ENERGY_JUSTLENDING_SUPPLY_ENERGY=...

# ========== 私钥配置（可选，仅用于开发/测试） ==========
# 警告：生产环境不要使用此配置！
# NEXT_PUBLIC_PRIVATE_KEY=0x...
